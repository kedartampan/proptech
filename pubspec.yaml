name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.13  # Tambahkan dependensi Shared Preferences
  sqflite: ^2.0.0+4   # Tambahkan ini
  path: ^1.8.0       # Tambahkan ini
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4  # Tambahkan firebase_auth di sini

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/profile.jpg
    - assets/property1.jpg
    - assets/property2.jpg
    - assets/property3.jpg
    - assets/property4.jpg
    - assets/property5.jpg
